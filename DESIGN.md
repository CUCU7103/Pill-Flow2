# Design

## Source of truth

- **상태:** Active · **작성일:** 2026-09-25
- **대상:** PillFlow 웹과 Capacitor Android 앱의 로그인, 오늘, 약 추가, 통계, 설정 화면.
- **근거:** `artifacts/pillflow/src/App.tsx`, `src/components/views/{LoginView,TodayView,AddView,StatsView}.tsx`, `src/components/common/BottomNav.tsx`, `src/index.css`, `src/hooks/use-theme.ts`, `artifacts/pillflow/capacitor.config.ts`, `docs/architecture.md`, `pillflow-login.png`를 확인했다. `src/` 경로는 이 문서에서 `artifacts/pillflow/src/`를 뜻한다. 기존 루트 디자인 문서, Storybook, 화면별 시각 기준 이미지는 없다.
- **외부 참고:** [똑구 소개 사이트](https://www.ddokgu.com/)의 쉬운 가치 설명과 단계별 소개, [토스 제품 원칙](https://toss.im/tossfeed/article/tossproductprinciples)의 쉬운 용어·명확한 행동·한 화면 한 메시지, [Toss Design System 운영 사례](https://toss.tech/article/toss-design-system)의 일관된 컴포넌트 패턴. 이들은 영감의 출처이며 브랜드나 화면을 복제하는 기준은 아니다.
- **적용 범위:** 이 저장소의 PillFlow를 개선하며 똑구와 토스는 참고 사례로 사용한다. 사용자가 시안 기반 앱 구현을 요청했다.
- **Figma 구현 기획서:** [PillFlow · 앱 리디자인 구현 기획서](https://www.figma.com/design/2nYZBlMJduRwYiSn8ktD1k?node-id=2-2). 목표, 화면 구조, 화면별 요구, 시각 규칙, 상태·접근성, 개발 순서와 시안 이미지를 함께 관리한다.

## Brand

- **성격:** 차분하고 친절한 복약 동반자. 금융 앱처럼 정보를 빠르게 읽을 수 있지만 의료 결정을 대신하는 듯한 인상은 피한다.
- **신뢰 신호:** 실제 저장된 일정·기록만 수치로 표시하고, 복용 상태·알림 상태·동기화 결과를 분명한 말로 알린다.
- **피할 것:** 과도한 보라색 그라디언트와 그림자, 장식용 배지, 근거 없는 연속 기록, 의학적 효능을 암시하는 문구. 기존 `TodayView.tsx`의 고정된 “연속 7일”과 검증되지 않은 성취 문구는 이번 구현에서 제거했다.

## Product goals

- **목표:** 첫 3초 안에 오늘 남은 약과 가장 가까운 복용 시간을 파악하고, 복용 기록을 한 번의 분명한 행동으로 남길 수 있게 한다. 약 추가에서 이름·시간·요일을 오류 없이 설정하고, 통계에서 기록의 의미를 쉽게 이해하게 한다.
- **비목표:** 진단·처방·복용량 추천, 구독·결제 관리, 불필요한 소셜/게임 기능 추가.
- **성공 신호:** 오늘 화면에서 다음 복용 정보 식별 시간, 복용 완료 작업 성공률, 약 추가 완료율, 알림 권한 및 사진 분석 실패 후 복구율. 실제 목표 수치는 사용자 관찰 후 정한다.

## Personas and jobs

- **주 사용자:** 매일 한 가지 이상 약이나 영양제 복용을 기록하는 사람. 이동 중 한 손으로 오늘 일정과 완료 여부를 확인한다.
- **보조 사용자 가정:** 여러 약을 관리하는 사람, 작은 글씨에 불편을 겪는 사람. 가족 계정/공유 관리는 현재 코드에서 확인되지 않았으므로 별도 검증 전 핵심 가치로 내세우지 않는다.
- **핵심 작업:** “지금 무엇을 복용해야 하나”, “먹었다는 기록을 남기고 싶다”, “새 약의 시간과 요일을 설정하고 싶다”, “지난주에 얼마나 지켰나”.

## Information architecture

- **로그인:** 앱 가치 한 문장 → 가능한 기능 2~3개 → Google 시작 버튼 → 동기화 설명.
- **오늘:** 날짜/설정 → 다음 복용과 남은 건수 → 시간순 복용 목록 → 보조 약 추가. 완료 상태는 같은 목록에서 확인한다.
- **약 추가:** 기존 3단계 구조를 유지하되 이름/종류/용량 → 시간/요일 → 확인/저장 순서와 현재 단계, 저장 전 검토를 명확히 한다. 사진 인식은 선택적 보조 수단이다.
- **통계:** 이번 주 요약 → 날짜별 기록 → 약별 상태. 수치의 기간과 분모를 표시한다.
- **탐색:** 현재 `오늘 / 추가 / 통계` 하단 탭과 설정 모달을 유지한다. 상세·삭제는 해당 약 행의 맥락 메뉴나 상세 화면에 둔다.

## Design principles

1. **다음 행동을 먼저 보여준다.** 오늘의 큰 퍼센트보다 다음 복용 시간과 약 이름이 우선이다.
2. **한 화면의 주 행동을 선명하게 한다.** 복용 행의 완료 버튼, 추가 화면의 다음/저장 버튼을 쉽게 찾게 한다.
3. **상태를 색 외에도 말로 표현한다.** 완료, 예정, 지연, 저장 중, 실패를 문구와 아이콘으로 같이 표시한다.
4. **정보는 실제 기록과 일치시킨다.** 계산할 수 없는 연속 일수나 성취는 보이지 않는다.
5. **디자인 규칙은 하나로 관리한다.** 색·간격·타이포그래피는 기존 `src/index.css` 토큰을 확장하고 화면의 하드코딩 색상은 점진적으로 교체한다.

## Visual language

- **색:** 따뜻한 화이트 `#FFFFFF`, 캔버스 `#F7F8FA`, 진한 텍스트 `#191F28`, 보조 텍스트 `#6B7684`, 경계 `#E5E8EB`. 시안 블루 `#3182F6`을 참고하되 작은 흰 글자가 들어가는 주 행동에는 명암비를 위해 `#1B64DA`를 사용한다. 옅은 블루는 `#EAF3FF`. 성공 `#18845A`, 주의 `#A05A00`, 오류 `#D64545`는 의미가 있는 상태에만 쓴다. 브랜드 고유성은 로고/콘텐츠에서 유지하고 토스의 자산을 사용하지 않는다.
- **글꼴:** 한국어 본문은 시스템 한글 글꼴과 현재 Plus Jakarta Sans의 숫자/영문을 조합한다. 화면 제목 28/34, 카드 제목 18/26, 본문 16/24, 보조 14/21, 캡션 최소 12/18px. 본문에 10px를 쓰지 않는다.
- **간격:** 4px 기반; 화면 좌우 20px, 큰 섹션 사이 24~32px, 관련 행 사이 8~12px. 360px 너비에서도 줄바꿈이 자연스러워야 한다.
- **모양과 깊이:** 카드 반경 20~24px, 버튼 14~16px, 얇은 경계와 매우 약한 그림자. 큰 그라디언트는 제거하고 평면 색과 여백으로 계층을 만든다.
- **움직임:** 복용 완료 피드백 150~250ms 수준. 현재 Framer Motion 전환은 `prefers-reduced-motion`을 존중해야 한다.
- **이미지와 아이콘:** 기존 Lucide/MedIcon을 일관된 크기·두께로 사용한다. 약 사진이 없는 상태에서도 구별 가능한 텍스트가 우선이다.

## Components

- **재사용:** `BottomNav`, `MedIcon`, `FormField`, `TimePicker`, 기존 Radix 기반 UI 컴포넌트와 설정/확인 모달.
- **정리할 패턴:** `NextDoseSummary`(다음 복용과 남은 건수), `MedicationRow`(이름·용량·시간·상태·완료), `SectionHeader`, `PrimaryAction`, `StatusMessage`. 이는 제안 패턴이며 구현 때 기존 화면의 중복이 실제로 생길 때만 공통 컴포넌트로 추출한다.
- **상태:** 각 행은 예정/완료/지난 일정, 버튼은 기본/눌림/로딩/비활성, 사진 분석은 진행/성공/실패를 제공한다. 삭제는 주 행동 옆의 항시 노출 버튼으로 두지 않는다.
- **토큰 소유:** `src/index.css`가 시각 토큰의 단일 소유자다. `src/hooks/use-theme.ts`의 중복 색상과 화면의 인라인 색상은 구현 단계에서 동일 토큰 참조로 옮긴다. 새 UI 라이브러리는 추가하지 않는다.

## Accessibility

- **목표:** WCAG 2.2 AA 수준. 일반 텍스트 4.5:1, 큰 텍스트 및 핵심 UI 경계 3:1을 검증한다.
- **조작:** 터치 대상 최소 44×44px, 키보드 탐색 순서와 보이는 포커스, 상태 변경의 스크린리더 알림을 제공한다. 아이콘만 있는 버튼에는 행동 이름을 넣는다.
- **읽기:** 시스템 글꼴 확대와 브라우저 확대를 허용한다. `index.html`의 `maximum-scale=1`은 이번 구현에서 제거했다.
- **감각:** 완료/오류를 색만으로 전하지 않고, 과한 움직임을 줄이며 Android 알림·진동 선호를 존중한다.

## Responsive behavior

- **기본:** 360~430px 모바일 한 열, 안전 영역과 하단 탭을 고려한다. 320px에서도 가로 스크롤 없이 핵심 작업을 할 수 있어야 한다.
- **큰 화면:** 기존 `max-w-md` 중심 셸은 유지하되 빈 데스크톱 화면을 과도하게 늘리지 않는다. 정식 데스크톱 레이아웃은 별도 범위 결정이 필요하다.
- **입력:** 터치에서 행 전체와 명확한 완료 버튼을 구분한다. 마우스 hover는 보조 피드백이며 필수 정보 전달 수단이 아니다.

## Interaction states

- **로딩:** 앱 시작과 저장에는 문맥이 있는 스켈레톤 또는 진행 문구를 쓴다. 중복 저장을 막는다.
- **빈 화면:** 등록된 약이 없으면 “첫 약 등록하기”, 오늘 복용할 약만 없으면 “오늘 일정이 없어요”를 구분한다.
- **오류:** 저장·동기화·사진 분석·알림 권한 실패는 원인과 재시도 행동을 같은 화면에서 제공한다.
- **성공:** 복용 완료 후 행의 상태와 요약이 즉시 갱신되고 취소 방법을 제공한다.
- **오프라인/느린 연결:** 현재 보장 동작은 확인되지 않았다. 구현 전 저장 실패와 재시도 정책을 확인한다.

## Content voice

- **어조:** 친절하고 짧은 존댓말. “오늘 남은 약 2개”, “오후 1시에 비타민 D를 복용해요”처럼 시간·행동·대상을 구체적으로 쓴다.
- **용어:** `복용 완료`, `복용 취소`, `약 추가`, `복용 시간`, `이번 주 기록`을 일관되게 사용한다. 통계는 기간과 계산 기준을 함께 보여준다.
- **금지:** “완벽한 건강”, 근거 없는 절약·개선 수치, 실제로 없는 가족 공유 기능, 죄책감을 주는 미복용 표현.

## Implementation constraints

- React 19, Vite 7, Tailwind CSS v4, Framer Motion, Recharts, Capacitor 8 Android와 기존 Supabase 데이터 모델을 사용한다. 새 의존성은 필요하지 않다.
- UI 전환은 현재 `App.tsx`의 상태 기반 화면 구조를 기준으로 한다. 실서비스 변경 때 브라우저와 Android WebView의 안전 영역·상태바·하단 탐색을 확인한다.
- 시안은 [`docs/design-concept/pillflow-concept.html`](docs/design-concept/pillflow-concept.html)에 독립된 정적 예시로 둔다. 예시 데이터와 화면은 실제 기능이나 의료 안내가 아니다.
- 데이터 계약과 시안의 차이는 [`docs/design-concept/backend-contract-gaps.md`](docs/design-concept/backend-contract-gaps.md)에 기록한다.
- 구현 검증 기준: 320/360/430px, 다크 모드, 글꼴 확대, 키보드, reduced motion, 빈/오류/로딩/완료 상태를 확인하고 `pnpm --filter @workspace/pillflow typecheck` 및 `build`를 실행한다.

## Open questions

- [ ] **제품 소유자:** 핵심 대상 사용자의 연령/접근성 요구와 브랜드 색상 선호 확인. 타이포그래피와 색상 확정에 영향을 준다.
- [ ] **제품/개발:** 하루 여러 복용 시간의 완료 기록 단위와 연속 일수 계산 기준 확인. 오늘 요약과 통계의 신뢰성에 영향을 준다.
- [ ] **제품/개발:** 가족 정보 관리가 실제 제공되는지 확인. 로그인 화면의 기능 문구에 영향을 준다.
- [ ] **제품/개발:** 오프라인 저장 및 동기화 실패 정책 확인. 상태 문구와 재시도 흐름에 영향을 준다.
