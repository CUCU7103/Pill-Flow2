# 애플리케이션 런타임 DB (Supavisor session 모드 URL 권장)
DB_URL=jdbc:postgresql://localhost:5432/postgres
DB_USERNAME=pillflow_api
DB_PASSWORD=로컬에서_설정
# Flyway DDL 전용 관리자 계정
FLYWAY_USERNAME=postgres
FLYWAY_PASSWORD=로컬에서_설정
# Supabase 프로젝트 URL (JWT JWKS/issuer 구성)
SUPABASE_URL=https://igyydnnehdjrwujxqdry.supabase.co
# 쉼표로 구분된 CORS 허용 origin
CORS_ALLOWED_ORIGINS=https://pillflow-app.vercel.app,https://localhost,https://pillflow.app
# migrate 프로파일(배포 파이프라인) 전용 — 운영에서는 SSM /pillflow/prod/migration/* 에서 주입된다
FLYWAY_URL=jdbc:postgresql://localhost:5432/postgres
